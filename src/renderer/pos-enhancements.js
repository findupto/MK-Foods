(() => {
  const heldKey = 'mk-foods-held-orders-v1';
  const getHeld = () => { try { return JSON.parse(localStorage.getItem(heldKey) || '[]'); } catch (_) { return []; } };
  const setHeld = rows => localStorage.setItem(heldKey, JSON.stringify(rows));
  const num = v => Math.max(0, Number(v || 0));
  const inlineNotice = (message, type = 'error') => {
    let box = document.getElementById('globalInlineNotice');
    if (!box) { box = document.createElement('div'); box.id = 'globalInlineNotice'; box.className = 'notice'; document.querySelector('main')?.prepend(box); }
    box.className = `notice ${type}`; box.textContent = message; box.hidden = false;
    clearTimeout(box._timer); box._timer = setTimeout(() => { box.hidden = true; }, 4500);
  };
  const calc = () => {
    const subtotal = cart.reduce((s, x) => s + num(x.price) * num(x.qty), 0);
    const discountType = document.getElementById('discountType')?.value || 'fixed';
    const discountInput = num(document.getElementById('discount')?.value);
    const discount = Math.min(discountType === 'percent' ? subtotal * Math.min(discountInput, 100) / 100 : discountInput, subtotal);
    const taxable = Math.max(0, subtotal - discount);
    const tax = taxable * num(db.settings?.tax) / 100;
    const deliveryFee = document.getElementById('orderType')?.value === 'Delivery' ? num(document.getElementById('deliveryFee')?.value) : 0;
    return { subtotal, discount, tax, deliveryFee, total: taxable + tax + deliveryFee };
  };
  const renderTotals = () => { const t = calc(), box = document.getElementById('posTotals'); if (!box) return; box.innerHTML = `<div><span>Subtotal</span><b>${money(t.subtotal)}</b></div><div><span>Discount</span><b>-${money(t.discount)}</b></div><div><span>Tax (${num(db.settings?.tax)}%)</span><b>${money(t.tax)}</b></div>${t.deliveryFee ? `<div><span>Delivery</span><b>${money(t.deliveryFee)}</b></div>` : ''}<div class="total"><span>Total</span><strong>${money(t.total)}</strong></div>`; };
  const renderHeld = () => { const rows = getHeld(), box = document.getElementById('heldOrders'); if (!box) return; box.innerHTML = rows.length ? rows.map((o,i)=>`<div class="dispatch"><div><b>${esc(o.name||o.id)}</b><div class="muted">${o.items.length} item(s) · ${money(o.total)}</div></div><div class="toolbar"><button class="mini" onclick="resumeHeld(${i})">Resume</button><button class="mini danger" onclick="deleteHeld(${i})">Delete</button></div></div>`).join('') : '<p class="muted">No held orders.</p>'; };
  const renderCart = () => { const box=document.getElementById('cartLines'); if(!box)return; box.innerHTML=cart.length?cart.map((x,i)=>`<div class="orderline"><div><b>${esc(x.name)}</b><small class="muted">${x.note?` · ${esc(x.note)}`:''}</small><div class="toolbar"><button class="mini" onclick="changeCartQty(${i},-1)">−</button><span>${x.qty}</span><button class="mini" onclick="changeCartQty(${i},1)">+</button><button class="mini secondary" onclick="editCartNote(${i})">Note</button></div></div><span>${money(num(x.price)*num(x.qty))}<button class="mini danger" onclick="removeItem(${i})">×</button></span></div>`).join(''):'<p class="muted">No items yet</p>'; renderTotals(); };
  window.changeCartQty=(i,delta)=>{const x=cart[i];if(!x)return;const p=db.products.find(p=>p.id===x.id),next=num(x.qty)+delta;if(next<=0)return removeItem(i);if(p&&next>num(p.stock)){inlineNotice('Not enough stock.');return}x.qty=next;renderCart();};
  window.editCartNote=i=>{if(!cart[i])return;const panel=document.getElementById('posInlineEditor');if(panel){panel.innerHTML=`<label><b>Item note / modifier</b><input id="inlineNote" class="field" value="${esc(cart[i].note||'')}" autofocus></label><div class="toolbar"><button class="btn" onclick="saveCartNote(${i})">Save Note</button><button class="btn secondary" onclick="closePosEditor()">Cancel</button></div>`;panel.hidden=false;panel.scrollIntoView({behavior:'smooth',block:'nearest'});}};
  window.saveCartNote=i=>{if(cart[i])cart[i].note=document.getElementById('inlineNote')?.value.trim()||'';closePosEditor();renderCart();}; window.closePosEditor=()=>{const p=document.getElementById('posInlineEditor');if(p)p.hidden=true;};
  window.holdCurrentOrder=()=>{if(!cart.length){inlineNotice('Add at least one item before holding the order.');return}const panel=document.getElementById('posInlineEditor');if(!panel)return;panel.innerHTML=`<label><b>Hold name / order number</b><input id="holdName" class="field" value="Hold ${new Date().toLocaleTimeString()}"></label><div class="toolbar"><button class="btn" onclick="saveHeldOrder()">Hold Order</button><button class="btn secondary" onclick="closePosEditor()">Cancel</button></div>`;panel.hidden=false;panel.scrollIntoView({behavior:'smooth',block:'nearest'});};
  window.saveHeldOrder=()=>{const name=document.getElementById('holdName')?.value.trim();if(!name){inlineNotice('Enter a hold name or order number.');return}const t=calc(),rows=getHeld();rows.push({id:`H-${Date.now()}`,name,items:cart.map(x=>({...x})),total:t.total,createdAt:new Date().toISOString()});setHeld(rows);cart.length=0;closePosEditor();render();};
  window.resumeHeld=i=>{const rows=getHeld(),o=rows[i];if(!o)return;if(cart.length){const panel=document.getElementById('posInlineEditor');if(!panel)return;panel.innerHTML=`<div class="notice"><b>Current order has items.</b><p class="muted">Replace the current order with “${esc(o.name)}”?</p><div class="toolbar"><button class="btn danger" onclick="confirmResumeHeld(${i})">Replace Current Order</button><button class="btn secondary" onclick="closePosEditor()">Keep Current Order</button></div></div>`;panel.hidden=false;return}confirmResumeHeld(i);};
  window.confirmResumeHeld=i=>{const rows=getHeld(),o=rows[i];if(!o)return;cart.length=0;o.items.forEach(x=>cart.push({...x}));rows.splice(i,1);setHeld(rows);closePosEditor();render();};
  window.deleteHeld=i=>{const rows=getHeld();if(!rows[i])return;const panel=document.getElementById('posInlineEditor');if(!panel)return;panel.innerHTML=`<div class="notice"><b>Delete held order?</b><p class="muted">${esc(rows[i].name||rows[i].id)} will be removed.</p><div class="toolbar"><button class="btn danger" onclick="confirmDeleteHeld(${i})">Delete Held Order</button><button class="btn secondary" onclick="closePosEditor()">Cancel</button></div></div>`;panel.hidden=false;};
  window.confirmDeleteHeld=i=>{const rows=getHeld();if(!rows[i])return;rows.splice(i,1);setHeld(rows);closePosEditor();renderHeld();};
  window.printReceipt=order=>{const items=Array.isArray(order.items)?order.items:[],w=window.open('', '_blank','width=420,height=700');if(!w){inlineNotice('Receipt print window was blocked by the operating system. Use Print Center instead.');return}const itemRows=items.map(i=>`<tr><td>${esc(i.name)}${i.note?`<br><small>${esc(i.note)}</small>`:''}</td><td>${i.qty}</td><td>${money(num(i.price)*num(i.qty))}</td></tr>`).join('');w.document.write(`<!doctype html><html><head><title>${esc(order.id)}</title><style>body{font-family:Arial,sans-serif;width:72mm;margin:0 auto;padding:8px;font-size:12px}h2{text-align:center;margin:0 0 6px}.muted{color:#666}table{width:100%;border-collapse:collapse}td{padding:3px 0;vertical-align:top}td:nth-child(2),td:nth-child(3){text-align:right}.line{border-top:1px dashed #777;margin:6px 0}.total{font-size:16px;font-weight:700;display:flex;justify-content:space-between}.center{text-align:center}</style></head><body><h2>${esc(db.settings?.business||'MK Foods POS')}</h2><div class="center">${esc(db.settings?.phone||'')}</div><div class="line"></div><div><b>${esc(order.id)}</b><br>${new Date(order.createdAt).toLocaleString()}</div><div class="line"></div><table>${itemRows}</table><div class="line"></div><div>Subtotal: ${money(order.subtotal)}</div><div>Discount: -${money(order.discount)}</div><div>Tax: ${money(order.tax)}</div>${num(order.deliveryFee)?`<div>Delivery: ${money(order.deliveryFee)}</div>`:''}<div class="total"><span>Total</span><span>${money(order.total)}</span></div><div class="line"></div><div>Payment: ${esc(order.payment)} · ${esc(order.paymentStatus||'pending')}</div><div class="center" style="margin-top:10px">Thank you!</div><script>window.onload=()=>{window.print();setTimeout(()=>window.close(),300)}</script></body></html>`);w.document.close();};
  window.checkoutEnhanced=async()=>{if(!cart.length)return;const t=calc(),type=document.getElementById('orderType')?.value||'Takeaway',payment=document.getElementById('payment')?.value||'Cash',address=document.getElementById('deliveryAddress')?.value.trim()||'',customerName=document.getElementById('customerName')?.value.trim()||'',tableId=document.getElementById('tableId')?.value||'';if(type==='Delivery'&&!address){inlineNotice('Delivery address is required.');return}let tenders=[{method:payment,amount:t.total}],paidAmount=payment==='Cash'?t.total:0;if(payment==='Split'){const panel=document.getElementById('posInlineEditor');if(!panel)return;panel.innerHTML=`<div class="formgrid"><label>Cash Amount<input id="splitCash" class="field" type="number" min="0" step="0.01" value="${t.total}"></label><label>Card Amount<input id="splitCard" class="field" type="number" min="0" step="0.01" value="0"></label><label>Online Amount<input id="splitOnline" class="field" type="number" min="0" step="0.01" value="0"></label></div><p class="muted">Total due: ${money(t.total)}</p><div class="toolbar"><button class="btn" onclick="saveSplitPayment(${t.total})">Continue</button><button class="btn secondary" onclick="closePosEditor()">Cancel</button></div>`;panel.hidden=false;return}
    await completeSale(t,{type,payment,address,customerName,tableId,tenders,paidAmount});};
  window.saveSplitPayment=async total=>{const cash=num(document.getElementById('splitCash')?.value),card=num(document.getElementById('splitCard')?.value),online=num(document.getElementById('splitOnline')?.value);if(Math.abs(cash+card+online-total)>0.01){inlineNotice(`Split payment must equal ${money(total)}.`);return}closePosEditor();const t=calc();await completeSale(t,{type:document.getElementById('orderType')?.value||'Takeaway',payment:'Split',address:document.getElementById('deliveryAddress')?.value.trim()||'',customerName:document.getElementById('customerName')?.value.trim()||'',tableId:document.getElementById('tableId')?.value||'',tenders:[{method:'Cash',amount:cash},{method:'Card',amount:card},{method:'Online',amount:online}].filter(x=>x.amount>0),paidAmount:cash});};
  async function completeSale(t,{type,payment,address,customerName,tableId,tenders,paidAmount}){const paymentStatus=payment==='Cash'?'settled':payment==='COD'?'due':'pending_verification',order={id:`ORD-${Date.now()}`,createdAt:new Date().toISOString(),items:cart.map(({id,name,price,qty,note})=>({id,name,price,qty,note:note||''})),subtotal:t.subtotal,discount:t.discount,tax:t.tax,deliveryFee:t.deliveryFee,total:t.total,payment,paymentStatus,tenders,paidAmount,orderType:type,address,customerName,tableId,status:'new'};const r=await api(window.mkFoods.createOrder,order);if(r?.ok===false){handleAuthError(r);return}cart.length=0;await load();renderHeld();const panel=document.getElementById('posInlineEditor');if(panel){panel.innerHTML=`<div class="notice success"><b>Order ${esc(order.id)} saved.</b><p class="muted">The sale has been recorded successfully.</p><div class="toolbar"><button class="btn" onclick="printReceiptFromPanel('${esc(order.id)}')">Print Receipt</button><button class="btn secondary" onclick="closePosEditor()">Done</button></div></div>`;panel.hidden=false;}};
  window.printReceiptFromPanel=id=>{const o=(db.orders||[]).find(x=>x.id===id);if(o)window.printReceipt(o);};
  views.pos=v=>{const cats=[...new Set((db.products||[]).filter(p=>p.available&&num(p.stock)>0).map(p=>p.category))];v.innerHTML=shell('POS / Orders','Complete billing · tax · discounts · tables · delivery · split tenders · hold/resume',`<div class="poslayout"><div class="panel"><div class="toolbar"><input id="productSearch" class="field" placeholder="Search menu..." oninput="renderPosProducts()"><select id="posCat" class="field" onchange="renderPosProducts()"><option value="">All categories</option>${cats.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('')}</select></div><div id="posProducts" class="productgrid"></div><div class="panel" style="margin-top:12px"><h3>Held Orders</h3><div id="heldOrders"></div></div></div><div class="panel cartpanel"><h2>Current Order</h2><div id="posInlineEditor" class="inline-editor" hidden></div><div id="cartLines"></div><div class="formgrid"><label>Type<select id="orderType" class="field"><option>Dine-in</option><option>Takeaway</option><option>Delivery</option></select></label><label>Payment<select id="payment" class="field"><option>Cash</option><option>Card</option><option>Online</option><option>COD</option><option>Split</option></select></label><label>Discount Type<select id="discountType" class="field"><option value="fixed">Fixed</option><option value="percent">Percentage</option></select></label><label>Discount<input id="discount" class="field" type="number" min="0" step="0.01" value="0"></label><label>Customer<input id="customerName" class="field" placeholder="Optional"></label><label>Dine-in Table<select id="tableId" class="field"><option value="">No table</option>${(db.tables||[]).filter(t=>t.status!=='Paid').map(t=>`<option value="${esc(t.id)}">${esc(t.name)} · ${esc(t.status)}</option>`).join('')}</select></label><label class="wide">Delivery Address<input id="deliveryAddress" class="field" placeholder="Required for delivery"></label><label class="wide">Delivery Fee<input id="deliveryFee" class="field" type="number" min="0" step="0.01" value="0"></label></div><div id="posTotals"></div><div class="toolbar"><button class="btn secondary" onclick="holdCurrentOrder()" ${cart.length?'':'disabled'}>Hold Order</button><button class="btn widebtn" ${cart.length?'':'disabled'} onclick="checkoutEnhanced()">Complete Sale</button></div></div></div>`);document.getElementById('orderType').addEventListener('change',renderTotals);document.getElementById('discountType').addEventListener('change',renderTotals);document.getElementById('discount').addEventListener('input',renderTotals);document.getElementById('deliveryFee').addEventListener('input',renderTotals);renderPosProducts();renderCart();renderHeld();};
  window.renderPosProducts=()=>{const el=document.getElementById('posProducts');if(!el)return;const q=(document.getElementById('productSearch')?.value||'').toLowerCase(),cat=document.getElementById('posCat')?.value||'';el.innerHTML=(db.products||[]).filter(p=>p.available&&num(p.stock)>0&&(!cat||p.category===cat)&&(!q||String(p.name).toLowerCase().includes(q))).map(p=>`<button class="product" onclick="add('${esc(p.id).replace(/'/g,'&#39;')}')"><b>${esc(p.name)}</b><span>${esc(p.category)} · ${money(p.price)} · Stock ${num(p.stock)}</span></button>`).join('')||'<p class="muted">No matching items.</p>';};
  window.add=id=>{const p=db.products.find(x=>x.id===id);if(!p||!p.available||num(p.stock)<=0){inlineNotice('Item is out of stock.');return}const existing=cart.find(x=>x.id===id);if(existing){if(num(existing.qty)>=num(p.stock)){inlineNotice('Not enough stock.');return}existing.qty+=1}else cart.push({...p,qty:1,note:''});render();};
  window.removeItem=i=>{cart.splice(i,1);render();};
})();
