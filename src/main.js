const{app,BrowserWindow,ipcMain}=require('electron');const path=require('path');const{createStore}=require('./store');let store;function createWindow(){const win=new BrowserWindow({width:1440,height:900,minWidth:1100,minHeight:700,backgroundColor:'#f6f7f5',webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false,sandbox:true}});win.loadFile(path.join(__dirname,'renderer','index.html'))}app.whenReady().then(()=>{store=createStore(app.getPath('userData'));ipcMain.handle('app:info',()=>({name:'MK Foods POS',business:store.get().settings.business,mode:'offline-first',version:app.getVersion()}));ipcMain.handle('auth:login',(_,u,p)=>store.authenticate(u,p));ipcMain.handle('db:snapshot',()=>store.get());ipcMain.handle('orders:create',(_,o)=>store.addOrder(o));ipcMain.handle('audit:add',(_,e)=>store.addAudit(e));ipcMain.handle('settings:update',(_,s)=>store.updateSettings(s));createWindow();app.on('activate',()=>{if(BrowserWindow.getAllWindows().length===0)createWindow()})});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});
