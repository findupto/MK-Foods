# Sync Module

Owns offline-first event persistence, durable queues, idempotent cloud synchronization, conflict handling, retries, configuration versioning, reconciliation UI contracts, and edge health state.